import * as React from "react";
import { Header } from "../components/Header/Header";

export default {
  title: "Components/Header",
  component: Header,
};

export const Basic = () => (
  <>
    <Header title="Header" />
  </>
);
