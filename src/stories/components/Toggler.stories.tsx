import * as React from "react";
import { Toggler as TogglerComponent } from "../../components/Toggler";

export default {
  title: "Components/Toggler",
  component: TogglerComponent,
};

export const Toggler = () => (
  <>
    <TogglerComponent onChange={() => {}} />
  </>
);
